---
- name: Install chezmoi and shell packages
  become: true
  block:
    - name: Install shell tooling
      ansible.builtin.include_tasks: install.yaml
      tags: [install]

- name: Apply dotfiles machine-wide
  become: true
  block:
    - name: Apply dotfiles via chezmoi
      ansible.builtin.include_tasks: apply.yaml
      tags: [apply]
