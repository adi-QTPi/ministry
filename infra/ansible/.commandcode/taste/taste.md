- Agent and skill system files (`agents.md`, skills) live under `~/.agents`, **not** in the project `.commandcode` directory. Always check `~/.agents` first for the authoritative versions. Confidence: 0.9
- Ansible playbooks should be decomposed into domain-specific subfolders (e.g., `network/`, `ingress/`, `azure/`) with the main `site.yaml` using `import_playbook` to reference them rather than containing inline play definitions. Confidence: 0.85
- In decomposed Ansible playbooks, infrastructure connection boilerplate (e.g., Azure backend details) stays in the sub-playbook, while domain-customizable data (e.g., which secrets to fetch) should live in `site.yaml` and be passed as `vars` on the `import_playbook`. The decomposed playbook acts as a parameterized template. Confidence: 0.8
- Ansible playbooks should avoid hardcoding `hosts: all`; instead use a variable like `"{{ target | default('all') }}"` so the target host can be specified at runtime via `-e`. Defaulting to `all` is acceptable only as a fallback. Confidence: 0.8
- Uses `just` as the task runner for Ansible; key parameters like target host and SSH user should be surfaced as explicit, required recipe arguments (not hidden in variadic `*flags`), making `just` the primary interface for running playbooks. Confidence: 0.85
